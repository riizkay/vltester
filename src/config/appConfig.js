// konfigurasi aplikasi
export const CONFIG = {
  // ukuran maksimal untuk resize image (pixel)
  MAX_IMAGE_SIZE: 448,
  
  // kualitas kompresi image (0-100)
  IMAGE_QUALITY: 80,
  
  // format image output
  IMAGE_FORMAT: 'JPEG',
  
  // API endpoint (ganti dengan URL yang sebenarnya)
  API_ENDPOINT: 'YOUR_API_ENDPOINT_HERE',
  
  // timeout untuk API call (ms)
  API_TIMEOUT: 30000,
};

// export default CONFIG;
